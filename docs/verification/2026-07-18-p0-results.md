# P0 验证记录（2026-07-18）

验证分支：`feat/k12-teaching-assistant`

验证提交：`0f8fed2`（该提交只在已完成本地全量验证的代码上补充 Node `>=20.9.0` 运行时约束；同提交远端 CI 已通过）

## 自动化结果

| 检查 | 结果 |
|---|---|
| Vitest | 47 个文件，295 项通过 |
| ESLint | 通过 |
| TypeScript | `tsc --noEmit` 通过 |
| Next.js 生产构建 | 通过；`/`、`/lab`、`/progress` 与 6 个 API Route 正常生成 |
| 真实 Pyodide 冒烟 | `bubble-sort`、`image-classifier` 均通过 |
| Python | Core/device 26 项通过；仅有既有 Starlette TestClient 弃用警告 |
| Python compileall | 通过 |
| Compose | 使用非敏感占位变量执行 `docker compose config -q` 通过 |
| Git diff | `git diff --check` 通过，工作区干净 |
| 密钥扫描 | 当前提交、全部 Git 历史、Next 客户端静态产物均为 0 命中 |

GitHub Actions：[`29618388967`](https://github.com/MaomaoBuhuiJAVA/mambo-k12-ai-robot/actions/runs/29618388967)，`Web application` 与 `Core API and device agent` 两个 Job 均为 success。

## 非浏览器真实冒烟

- Core API 在 Windows 普通用户进程启动，OrangePi systemd agent 自动从退避状态恢复连接。
- Next.js `/api/device` 通过服务端令牌读取 Core，返回 `online`；浏览器响应没有管理令牌或完整状态。
- SQLite 时间戳缺失时区的问题在真实链路中被发现并修复为 UTC 输出。
- 当前板端仍运行旧的 `agent 0.1.0`，真实返回 `ping,get_status`；仓库 `agent 0.2.0` 已加入基于 `/dev` 节点的摄像头、ALSA、显示和 NPU 能力探测，但尚未经授权重启板端 systemd 服务。
- 本机网络无法连接 Google Generative Language API。聊天在单次上游失败后返回 74 字课程降级回答并包含 `[S1]`；绘本返回 4 页种子版本。由此验证了失败降级，不构成 Gemini 成功调用证据。
- DOCX（9972 bytes）和 PPTX（91760 bytes）经接口生成，并由本机 Microsoft Word / PowerPoint 隐藏只读打开成功；Word 1 页，PPT 6 页。
- NIST、PyTorch 两个页面和 scikit-learn 来源链接在验证时均返回 HTTP 200。

## 未执行与阻塞

- 按用户要求未执行浏览器/Playwright、三个视口、麦克风权限、视觉和可访问性手工验收。
- Vercel GitHub 集成已触发，但当前 Preview 失败：`dpl_Eqf6P7yPeyKWakiJsSqpauptsBBw`。GitHub 状态只公开失败和部署 ID；Vercel 日志 API/CLI 要求项目账户令牌，当前电脑没有登录凭据，因此不能诊断为代码、Root Directory、Node 项目设置或环境变量中的哪一项。
- 未配置可公网访问的 Core HTTPS/WSS、生产 PostgreSQL 与 Vercel Redis REST，因此不能宣称生产部署完成。
- Gemini 在本机网络未成功，必须在 Vercel 或其他可访问 Google API 的运行环境再次验证文本、图片和音频。

## 发布结论

代码与自动化达到可创建 Preview 的门槛；Vercel、真实浏览器和公网软硬件链路仍是发布阻断项。不得将本记录解释为 Production 已上线。
